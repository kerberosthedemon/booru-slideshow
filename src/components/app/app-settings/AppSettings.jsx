import React, { Component } from 'react';
import Typography from '@material-ui/core/Typography';

export default class AppSettings extends Component {
  render() {
    return (
      <Typography noWrap>Welcome to the Settings page!</Typography>
    )
  }
}
